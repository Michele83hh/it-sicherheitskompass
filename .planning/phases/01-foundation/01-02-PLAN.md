---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/language-switcher.tsx
  - src/messages/de.json
  - src/messages/en.json
  - public/favicon.svg
  - src/app/favicon.ico
autonomous: false

must_haves:
  truths:
    - "Language switcher is visible on every page and toggles between German and English"
    - "All visible text changes when switching language -- no hardcoded strings remain"
    - "Design looks professional: consistent colors, typography, spacing -- not a default Next.js template"
    - "Favicon shows NIS2 branding in browser tab, page title reads 'NIS2 Readiness Check'"
    - "No framework defaults visible (no Next.js logo, no Vercel branding, no 'Get started by editing')"
    - "Footer contains Rechtsstand placeholder and 'Keine Rechtsberatung' hint"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Navigation header with app name and language switcher"
      min_lines: 20
    - path: "src/components/layout/footer.tsx"
      provides: "Footer with legal date placeholder and disclaimer hint"
      min_lines: 15
    - path: "src/components/layout/language-switcher.tsx"
      provides: "DE/EN toggle component using next-intl routing"
      min_lines: 15
    - path: "src/app/globals.css"
      provides: "Design tokens: color palette, typography scale"
      contains: "--primary"
    - path: "public/favicon.svg"
      provides: "NIS2 branded favicon"
    - path: "src/app/[locale]/page.tsx"
      provides: "Professionally styled landing page"
      min_lines: 40
  key_links:
    - from: "src/components/layout/language-switcher.tsx"
      to: "src/lib/i18n/routing.ts"
      via: "Uses next-intl useRouter and useLocale for locale switching"
      pattern: "useRouter|useLocale|usePathname"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/language-switcher.tsx"
      via: "Header renders LanguageSwitcher component"
      pattern: "LanguageSwitcher"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "Layout renders Header and Footer around children"
      pattern: "Header|Footer"
---

<objective>
Build the professional design system and navigation shell: custom color palette, typography, header with language switcher, footer with legal placeholders, branded favicon, and a polished landing page that looks like a consulting tool -- not a student project or default template.

Purpose: This plan delivers the visual identity that makes the tool credible as a Bewerbungsprojekt and future consulting lead generator. The design system established here carries through all subsequent phases.
Output: A visually polished, professionally branded landing page with working DE/EN language switching, custom favicon, and consistent design tokens.
</objective>

<execution_context>
@C:\Users\mjoan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\mjoan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\mjoan\Desktop\nis2-readiness-check\.planning\PROJECT.md
@C:\Users\mjoan\Desktop\nis2-readiness-check\.planning\ROADMAP.md
@C:\Users\mjoan\Desktop\nis2-readiness-check\.planning\STATE.md
@C:\Users\mjoan\Desktop\nis2-readiness-check\.planning\research\STACK.md
@C:\Users\mjoan\Desktop\nis2-readiness-check\.planning\phases\01-foundation\01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, layout components, and favicon</name>
  <files>
    src/app/globals.css
    src/components/layout/header.tsx
    src/components/layout/footer.tsx
    src/components/layout/language-switcher.tsx
    public/favicon.svg
    src/app/favicon.ico
  </files>
  <action>
    1. Define the color palette in src/app/globals.css. The design should convey trust, professionalism, and compliance seriousness. Use a blue-based primary palette (trust/security) with a slate neutral scale. Override shadcn/ui CSS custom properties in the :root and .dark selectors.

       Color direction:
       - Primary: Deep blue (like #1e40af / hsl(222, 70%, 40%)) -- conveys trust, authority, compliance
       - Secondary/Accent: Teal or emerald for positive actions (hsl(162, 60%, 40%))
       - Background: Near-white (hsl(220, 20%, 98%)) -- not pure white, slightly warm
       - Cards: Pure white with subtle shadow
       - Text: Slate-900 for headings, Slate-600 for body
       - Destructive: Keep shadcn default red for errors
       - Traffic light colors (define as CSS vars for later use): --traffic-red: hsl(0, 72%, 51%), --traffic-yellow: hsl(45, 93%, 47%), --traffic-green: hsl(142, 71%, 45%)

       Typography direction:
       - Use Inter font (via next/font/google) -- clean, professional, excellent German character support
       - Font sizes: Use Tailwind defaults but ensure h1 is bold and commanding
       - Line height: Relaxed for body text (1.7), tight for headings (1.2)

       Spacing: Use Tailwind defaults. The key is consistent use, not custom values.

    2. Create src/components/layout/language-switcher.tsx:
       - A compact toggle between DE and EN
       - Use next-intl's useRouter, useLocale, and usePathname from 'next-intl/navigation' (create a navigation.ts if needed)
       - Visual: Two buttons side by side, active locale highlighted with primary color, inactive is muted
       - Clicking switches locale while preserving current pathname
       - Must use the routing config from src/lib/i18n/routing.ts via createNavigation

       Implementation pattern for next-intl v4:
       ```typescript
       // src/lib/i18n/navigation.ts
       import { createNavigation } from 'next-intl/navigation';
       import { routing } from './routing';

       export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
       ```

       Then in language-switcher.tsx:
       ```typescript
       'use client';
       import { useLocale } from 'next-intl';
       import { usePathname, useRouter } from '@/lib/i18n/navigation';
       ```

       The switcher calls router.replace(pathname, { locale: newLocale }).

    3. Create src/components/layout/header.tsx:
       - Fixed or sticky top navigation bar
       - Left: App name "NIS2 Readiness Check" (use translation key common.appName)
       - Right: LanguageSwitcher component
       - Background: white/background with subtle bottom border
       - Height: ~64px
       - Must be a client component (uses translations) OR a server component that passes translations down. Prefer: make it a client component with 'use client' and useTranslations.
       - Include lucide-react Shield icon next to the app name for visual identity

    4. Create src/components/layout/footer.tsx:
       - Compact footer at bottom of page
       - Content (all from i18n keys):
         - "Keine Rechtsberatung -- unverbindliche Orientierungshilfe" (disclaimer hint)
         - "Rechtsstand: [Datum wird ergaenzt]" (placeholder for legal date -- filled in Phase 7)
         - Copyright line with current year
       - Muted text color, small font size
       - Add these keys to de.json and en.json under a "footer" namespace

    5. Create public/favicon.svg:
       - Simple SVG favicon: A shield icon with "N2" text inside, using the primary blue color
       - Keep it clean and recognizable at 32x32 and 16x16
       - Example SVG: A rounded rectangle or shield shape in primary blue (#1e40af) with white "N2" text
       - Also generate/place a basic favicon.ico (can be a simple blue square with N2) at src/app/favicon.ico (Next.js App Router convention) or keep in public/

       NOTE: Next.js 16 App Router supports placing favicon.ico in src/app/ directory for automatic favicon handling. Place it there. The SVG goes in public/ as a higher-quality alternative referenced in metadata.

    6. Add footer translation keys to both message files. Under a new "footer" key:
       DE:
       ```json
       "footer": {
         "disclaimer": "Keine Rechtsberatung \u2014 unverbindliche Orientierungshilfe",
         "legalDate": "Rechtsstand: wird ergaenzt",
         "copyright": "\u00a9 {year} NIS2 Readiness Check"
       }
       ```
       EN:
       ```json
       "footer": {
         "disclaimer": "Not legal advice \u2014 non-binding orientation aid",
         "legalDate": "Legal status: to be updated",
         "copyright": "\u00a9 {year} NIS2 Readiness Check"
       }
       ```
  </action>
  <verify>
    - All four component files exist and compile: header.tsx, footer.tsx, language-switcher.tsx, navigation.ts
    - public/favicon.svg exists
    - `npx tsc --noEmit` returns zero errors
    - globals.css contains custom property definitions (--primary, --traffic-red, etc.)
  </verify>
  <done>
    Design tokens defined in globals.css. Header with app name and language switcher. Footer with disclaimer and legal date placeholder. Branded favicon SVG. All translation keys added. Zero TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire layout shell and polish landing page</name>
  <files>
    src/app/[locale]/layout.tsx
    src/app/[locale]/page.tsx
    src/messages/de.json
    src/messages/en.json
  </files>
  <action>
    1. Update src/app/[locale]/layout.tsx:
       - Import and use Inter font from next/font/google
       - Wrap body content with Header at top and Footer at bottom
       - Add the font className to the body element
       - Add antialiased class to body
       - Structure: html > body(font + antialiased) > Header > main(children) > Footer
       - Import globals.css (should already be imported from Plan 01)

       ```typescript
       import { Inter } from 'next/font/google';

       const inter = Inter({ subsets: ['latin'] });
       ```

       Body structure:
       ```tsx
       <body className={`${inter.className} antialiased`}>
         <NextIntlClientProvider messages={messages}>
           <Header />
           <main className="flex-1">{children}</main>
           <Footer />
         </NextIntlClientProvider>
       </body>
       ```
       Make body a flex column with min-h-screen so footer stays at bottom:
       `className={`${inter.className} antialiased flex min-h-screen flex-col`}`

    2. Rebuild src/app/[locale]/page.tsx into a professional landing page:

       The landing page must look like a consulting tool, not a template. Key sections:

       a) HERO SECTION:
          - Large heading: t('landing.hero.title') -- "Ist Ihr Unternehmen NIS2-ready?"
          - Subtitle paragraph with the value proposition
          - Primary CTA button: "Kostenlos pruefen" (large, prominent)
          - Secondary link: "Mehr erfahren" (text link or outlined button)
          - Background: Subtle gradient or pattern to distinguish from rest of page

       b) VALUE PROPOSITIONS (3 cards):
          - "100% Anonym" with Shield/Lock icon
          - "Rechtlich fundiert" with Scale/BookOpen icon
          - "Konkrete Empfehlungen" with CheckCircle/ClipboardList icon
          - Use shadcn Card components
          - Icons from lucide-react
          - Grid layout: 1 col mobile, 3 cols desktop

       c) HOW IT WORKS (3 steps):
          - Step 1: "Betroffenheit pruefen" -- Check if NIS2 applies
          - Step 2: "Luecken analysieren" -- Answer questions across 10 categories
          - Step 3: "Report erhalten" -- Get traffic-light results and PDF
          - Use numbered circles or step indicators
          - Add these keys to de.json and en.json under "landing.steps"

       d) TRUST SIGNALS:
          - "Basierend auf NIS2UmsG (BGBl. 2025 I Nr. 301)"
          - "EU-Richtlinie 2022/2555"
          - "BSI IT-Grundschutz"
          - Small text or badges at the bottom, before footer
          - Add these keys under "landing.trust"

       Add all new translation keys to both de.json and en.json.

       Styling guidelines:
       - Use max-w-6xl or max-w-7xl container, centered
       - Generous vertical padding between sections (py-16 to py-24)
       - Headings: text-4xl to text-5xl for hero, text-2xl to text-3xl for section headings
       - Body text: text-lg, text-muted-foreground
       - Cards: hover:shadow-md transition for polish
       - The page must look complete and intentional, not like a wireframe

    3. Ensure ALL visible text comes from i18n (useTranslations). Zero hardcoded strings.
       Check: No string literals in JSX except className values and icon names.

    4. Test the language switcher end-to-end:
       - Click DE -> all text is German
       - Click EN -> all text is English
       - URL changes between /de and /en
       - Page does not reload (client-side navigation)

    5. Verify favicon appears in browser tab. The page title should show "NIS2 Readiness Check".
  </action>
  <verify>
    - `npm run dev` starts and the landing page renders at /de with full professional styling
    - Language switcher toggles all visible text between DE and EN
    - Browser tab shows "NIS2 Readiness Check" title and favicon
    - No framework defaults visible (no Next.js logo, no "Get started by editing")
    - `npx tsc --noEmit` returns zero errors
    - Page has hero section, value proposition cards, how-it-works steps, and trust signals
  </verify>
  <done>
    Professional landing page renders at /de and /en. Header with language switcher visible on all pages. Footer with disclaimer visible. All text translates correctly between German and English. Favicon shows NIS2 branding. Design looks like a consulting tool, not a student project.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation: Professional landing page with design system, working DE/EN language switching, navigation header with language switcher, branded footer, and custom favicon. The page should look like a real compliance consulting tool.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in your browser -- it should redirect to /de
    2. Verify the landing page looks professional: clean typography, blue color scheme, proper spacing, no default template elements
    3. Check the hero section has a compelling headline and CTA button
    4. Verify the three value proposition cards are displayed
    5. Click the language switcher to toggle to English -- ALL visible text should change
    6. Click back to German -- all text returns to German
    7. Check the browser tab: favicon should show NIS2 branding, title should say "NIS2 Readiness Check"
    8. Check the footer: should show disclaimer text and legal date placeholder
    9. Open DevTools > Network tab: confirm no analytics requests, no tracking scripts
    10. Open DevTools > Application > Cookies: confirm no cookies are set

    Key question: Does this look like a professional consulting tool you would show in a Bewerbung? Or does it still look like a template/student project?
  </how-to-verify>
  <resume-signal>Type "approved" if design looks professional and everything works, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run dev` renders professional landing page at /de
2. Language switcher toggles all text between DE and EN without page reload
3. Header shows app name + language switcher on every page
4. Footer shows disclaimer + legal date placeholder
5. Favicon shows NIS2 branding in browser tab
6. Page title is "NIS2 Readiness Check" (not "Create Next App" or similar)
7. No cookies, no analytics, no tracking (verified in DevTools)
8. Design uses consistent color palette (blue-based primary, slate neutrals)
9. Typography is Inter font, clean and professional
10. All text comes from i18n -- zero hardcoded strings
11. `npx tsc --noEmit` zero errors
</verification>

<success_criteria>
- Professional design visible at /de: hero, value props, how-it-works, trust signals
- Language switcher toggles DE/EN with all text changing
- Custom favicon and page title (no framework defaults)
- Consistent design tokens (colors, typography, spacing)
- Footer with legal disclaimer placeholder
- User confirms design passes the "consulting tool, not student project" bar
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
